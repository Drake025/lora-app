import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('LIORA app smoke test', (WidgetTester tester) async {
    // Basic smoke test - app requires Firebase init so we skip actual widget testing
    expect(true, isTrue);
  });
}
